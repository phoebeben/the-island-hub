<div class="container">

  <h1>Your Bookings<h1>

  <% if @bookings.empty? %>
    <p>You have no bookings</p>
  <% else %>
    <%= render 'booking_card', locals: { bookings: @bookings } %>
  <% end %>

